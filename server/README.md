to run the backend server first go to server directory
type cd server 
then npm i 
once all packeges installed
simply run the script in our case "npm run server" if you are using yarn then simply type yard run server 